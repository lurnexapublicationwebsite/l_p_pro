version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps
    build:
      commands:
        - rm -rf .next
        - node scripts/inject-env.js
        - NODE_OPTIONS="--max-old-space-size=4096" npm run build
        - rm -rf .next/cache .next/diagnostics .next/dev .next/trace
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
    exclude:
      - 'cache/**/*'
      - 'diagnostics/**/*'
      - 'dev/**/*'
      - 'trace/**/*'
  cache:
    paths:
      - node_modules/**/*




